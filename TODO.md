# TODO
- [x] Create branch with a dev tool to try different fonts for the headers
  - Done on the `heading-fonts` branch (toolbar Fonts panel: Google and local families, sizes, weights, variable axes, Copy CSS).
- [x] Implement the fonts chosen with the `heading-fonts` tool
- [x] Subset fonts in HTML exports to the glyphs the document uses
  - Browser print-to-PDF already subsets and embeds web fonts, if PDF export is added.
- [ ] Put a `<Loading>` boundary around the preview, so it shows that it's loading on first load
  - Local image access (`src/state/local-images.ts`) is an async memo, and without a boundary the app's root mount waits for it (dev warns `ASYNC_OUTSIDE_LOADING_BOUNDARY`).
- [ ] Move the remaining async state to Solid 2's async model (see `.claude/skills/solid-async/SKILL.md`)
  - `state/documents.ts` file handles: `loadFileHandles` uses the `handleChanges` counter and retries when this tab changed a handle during the read, and `loadDocumentFile`/`findDocumentForFile` await the `handlesLoaded` promise. Make the handles a writable async signal (`createSignal(async () => readHandles())`) that `setFileHandle`/`removeFileHandle` set from actions and backup sync refreshes; readers then wait for the first load, and `handlesLoaded` and the counter go. The startup prune stays a side effect. The local-images access memo can then read the handles directly instead of falling back to `loadDocumentFile`.
  - `state/recent-files.ts`: a signal set after awaits, a `queue` promise chain so a load can't overwrite a newer change, and a manual reload on focus and visibility. Make the list a writable async signal refreshed on focus/visibility, with `rememberFile`/`forgetFile` as actions (a write inside an action survives a refresh in flight). Check whether the queue is still needed for overlapping read-modify-writes of IndexedDB; actions don't queue. The IndexedDB list pattern matches `state/granted-folders.ts`, so they may share a helper.
  - `components/Preview.tsx`: the `languagesLoaded` counter re-renders blocks once a code language loads, with `createMarkdownRenderer`'s `onLanguageLoad` callback and its skip-stale-outputs logic in `lib/markdown.ts`. Instead, a block's HTML could be a promise until its languages load, like local images. That holds a new code block back until its language loads instead of showing it unhighlighted first, so decide whether that's acceptable.
  - `state/documents.ts` export: the `exporting` signal plus the `exportRunning` flag. Make the export an action with a `createOptimistic(false)` busy flag that reverts when it settles. The flag is set from `exportHtml`'s `onBuild` callback after the save dialog, so the action needs to resume there (split picking the file from building it, or `yield` around the callback).
  - `state/file-changes.ts`: the effect restarts its timer on a key string of ids and names. Track `documentFile(doc)` for each document instead, now that handles are reactive. Polling, the `running` dedupe and the notices stay imperative.
  - `state/documents.ts` save, open and open-recent: async functions that write state after awaits (`saveActiveDocument`, `openDocument`, `openRecentFile`, `openFiles`, `openFileDocument`). Make them actions so their writes join one transition. `savingIds` could become an optimistic flag, though only `file-changes.ts` reads it.
